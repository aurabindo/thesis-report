
This directory contains sources for the presentation
To compile, run make. Only tested on Linux. 

This prentation uses `Beamer` class. Make sure
it is installed on your system.

Installing packages in Linux is quite easy if you
use OS's package managerlike apt for ubuntu or 
dnf for fedora.
